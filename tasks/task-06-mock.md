# Task 06 — `inquisitor-mock` + `inquisitor-mock-starter` (Reserved)

## Goal

Provide an embedded mock HTTP server that scenario tests can use to stub third-party APIs, removing the need for live external services during CI. `inquisitor-mock-starter` wires it into the Spring context automatically.

## Status

Reserved for future implementation — scope and API not yet defined.
