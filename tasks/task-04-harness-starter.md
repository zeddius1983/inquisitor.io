# Task 04 — `inquisitor-harness-starter` Module

## Goal

Provide Spring Boot 4 autoconfiguration for `inquisitor-harness`: bind `InquisitorProperties`, auto-create `ScenarioParser` and `ScenarioExecutor` beans, and expose the starter POM so consumers need a single dependency.

## Status

Pending — to be elaborated before implementation.
