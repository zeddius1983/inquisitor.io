# Task 02 — `inquisitor-harness` Module

## Goal

Implement the Spring AI-based core of Inquisitor: parse markdown scenario files into structured `Scenario` objects, orchestrate LLM tool-calling against a live HTTP target, and return a `ScenarioResult` with pass/fail verdict and reasoning.

## Status

Pending — to be elaborated before implementation.
